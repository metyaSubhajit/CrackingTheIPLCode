# IPL Data Analytics Project (2008-2020)
# Requirements file — install with: pip install -r requirements.txt
#
# Packages used directly in app.py
# (os, subprocess, warnings, zipfile are Python standard library — no install needed)

pandas>=2.0.0
numpy>=1.26.0
plotly>=5.18.0
streamlit>=1.32.0
scipy>=1.11.0
fpdf2>=2.7.0
python-docx>=1.1.0
kaggle>=1.6.0
